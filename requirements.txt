# Runtime dependencies for the pipeline (src/) and API (api/).
# Versions pinned to a known-good environment. For notebooks/figures and
# Jupyter tooling, see requirements-dev.txt.

# core data + modelling
numpy==2.4.6
pandas==3.0.3
scikit-learn==1.9.0
scipy==1.17.1
xgboost==3.2.0
joblib==1.5.3

# storage formats + cloud (local <-> S3 behind one interface)
pyarrow==24.0.0
fsspec==2026.6.0
s3fs==2026.6.0
boto3==1.43.0
botocore==1.43.0

# data download (TLS cert bundle for the NASA fetch)
certifi==2026.6.17
requests==2.34.2
