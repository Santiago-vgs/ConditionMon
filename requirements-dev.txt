# Development / exploration dependencies (notebooks + figure generation).
# Install on top of the runtime deps:  pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# notebooks
jupyter==1.1.1
jupyterlab==4.5.8
ipykernel==7.3.0
ipywidgets==8.1.8

# plotting extras (matplotlib itself is a runtime dep — see requirements.txt)
pillow==12.2.0

# tests
pytest==9.1.1
